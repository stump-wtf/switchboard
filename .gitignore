# Go build output
/bin/
/switchboard
*.exe
*.test
*.out
coverage.out

# Env / secrets — NEVER commit (secrets are injected via env/deployment config)
.env
.env.*
*.pem
*.key

# switchboard runtime data (kept out of git)
data/

# IDE / editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Agent worktrees
.claude/worktrees/

# SDD tracker-issue cache (per ADR-0025)
.sdd/

.claude/settings.local.json

# goreleaser build output.
dist/
